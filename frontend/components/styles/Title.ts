import styled from 'styled-components';

export const MainTitle = styled.h1`
  color: var(--black);
  font-size: 3rem;
  font-weight: 800;
  line-height: 4.06rem;
  text-transform: uppercase;
  // letter-spacing: 0em;

  span {
    color: var(--red);
  }
`;
